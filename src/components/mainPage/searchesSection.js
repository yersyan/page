import React from "react";
import {Title} from "./components";
import {GalleryMenu} from "./galleryMenu";

export const SearchesSection = ({categoryArray}) => {
    return <section className="searchesSection">
        <h2 className="title">Top Searches</h2>
    </section>
}